# TODO

## Completed

- **Bug: application does not run** — Fixed by switching tasks.json to use `python` command instead of hardcoded `.venv/bin/python` path.

## Backlog

- Feature: Config
  Add a `config.txt` file containing:
  - `MP3SPLIT_BINS_FOLDER`
  - `SPLIT_FIXED_DURATION_MINUTES`
