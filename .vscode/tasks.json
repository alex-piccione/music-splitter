{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "🏗️ Setup Project",
      "type": "shell",
      "command": "uv venv .venv --python 3.14 && uv pip install -r requirements.txt",
      "problemMatcher": []
    },
    {
      "label": "🚀 Run Music-Splitter",
      "type": "shell",
      "command": "if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif [ -x .venv/Scripts/python.exe ]; then PY=.venv/Scripts/python.exe; else echo 'ERROR: virtualenv not found. Run the \"Setup Project\" task first.'; exit 1; fi; exec \"$PY\" main.py",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "🧪 Run Tests",
      "type": "shell",
      "command": "if [ -x .venv/bin/python ]; then PY=.venv/bin/python; elif [ -x .venv/Scripts/python.exe ]; then PY=.venv/Scripts/python.exe; else echo 'ERROR: virtualenv not found. Run the \"Setup Project\" task first.'; exit 1; fi; exec \"$PY\" -m unittest discover tests",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    }
  ]
}
